# 操作説明

オトクリの画面とオーバーレイの働きを、項目ごとにまとめます。オフラインでもこのページが正本です。ネットが使えるときは、下の GitHub リンクと同じ内容をブラウザでも開けます。

## ホーム

- **他のアプリの上に表示** / **ユーザー補助**: 許可状態と、設定アプリへのショートカット
- **画面に置いて開始**: かんたん配置（点と棒）を対象アプリの上に出す
- **フローティング操作を表示**: 操作バブルだけを出す
- **かんたんモード**: 保存済みプロファイルの一覧と編集
- **記録モード**: マクロの記録・一覧・編集
- **チュートリアル** / **操作説明**: このヘルプ

## 設定

- オーバーレイ、ユーザー補助、アプリ情報（制限付き設定の解除はここから）
- 再生前カウントダウン秒数
- セットアップガイドのやり直し
- バージョンとパッケージ名（debug 版は `jp.otoclick.app.debug`）
- チュートリアル／操作説明へのリンク。GitHub の URL はコピーできます

## かんたん配置パネル

| 操作 | 内容 |
|---|---|
| クリック | タップ点を追加 |
| スワイプ | 下→上の棒を追加。ハンドルで方向と長さを変更 |
| 間隔 − / + | 再生間隔（50〜10000 ms） |
| 回数 − / + | 0 は無限 |
| 開始 | いまの配置を保存して再生 |
| 保存 | プロファイルとして残す |
| 閉じる | 点・棒・キャンバスを外してバブルへ戻る |

空いている画面は下のアプリにタッチが通ります。触るのは点・棒・パネルだけです。

## 記録パネル

| 操作 | 内容 |
|---|---|
| 記録を保存 | いまのジェスチャーをマクロにして記録終了 |
| キャンセル | 記録内容を捨てる。バブルは残る |
| 閉じる | 記録を捨て、**すべての**オーバーレイを消して通常状態へ |

閉じるはパネル先頭、画面下の赤いボタン、通知アクションの 3 か所にあります。どれも確認ダイアログなしです（貼りつき対策）。

## 通知

操作パネルが出ているあいだ、常駐通知があります。

- 記録中: **記録を保存** / **キャンセル** / **閉じる**
- それ以外: **閉じる**（オーバーレイ全体を終了）

パネルが画面の下に隠れたときも、通知から抜けられます。

## マクロ編集

記録したステップ（タップ・スワイプ・待機・繰り返し）を一覧で編集できます。画像マッチとシーン判定は次のバージョン用の枠だけで、まだ判定しません。

## 権限が通らないとき

1. オーバーレイがオフ → ホームの「許可」
2. ユーザー補助がオフ、またはグレー → アプリ情報から制限付き設定を許可して再トライ
3. 再生しても動かない → ユーザー補助のスイッチを一度オフ→オン
4. 記録画面が消えない → 通知の「閉じる」。それでも残る場合はアプリ情報から強制停止

## リンク（ネットがあるとき）

- チュートリアル: https://mikasahahappy0526-create.github.io/otoclick/tutorial
- 操作説明: https://mikasahahappy0526-create.github.io/otoclick/operations
- 同じ文書（raw）: https://raw.githubusercontent.com/mikasahahappy0526-create/otoclick/main/docs/tutorial.md
- 同じ文書（raw）: https://raw.githubusercontent.com/mikasahahappy0526-create/otoclick/main/docs/operations.md

アプリ内のページは通信なしで開けます。リンクは共有やブラウザ用です。
